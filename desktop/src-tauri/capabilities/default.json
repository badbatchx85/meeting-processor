{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window (event listen + invoke custom commands).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
